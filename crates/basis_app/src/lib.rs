//! Application layer for Basis tracker

// Placeholder for application functionality
pub struct BasisApp;

impl Default for BasisApp {
    fn default() -> Self {
        Self::new()
    }
}

impl BasisApp {
    pub fn new() -> Self {
        Self
    }
}
